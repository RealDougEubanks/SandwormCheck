---
name: False positive
about: The scanner flagged something legitimate
labels: false-positive
---

**Signature ID** (e.g. `SH25-R001`):

**Path that matched**:

**Why the file is legitimate** (what package ships it, and what it is for):

**Scanner output** (the finding lines, or `--json`; redact anything sensitive):

```
```

**Platform** (OS, and `sandwormcheck.sh --version`):
