---
name: Missed detection / new indicator
about: A compromised artifact the scanner does not detect
labels: detection
---

**What was present on the host** (filename, path, package version, or hash):

**Published source for this indicator** (vendor advisory, CSV, or registry
evidence — indicators are only encoded when traceable to a named source):

**Did the scanner exit 0?** If so, say so explicitly; a false clean is treated as
the most serious class of bug.

**Scanner output** (`--verbose` is most useful):

```
```
